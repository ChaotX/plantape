{
  "name": "plantape",
  "private": true,
  "type": "module",
  "description": "Garden survey with a tape measure: least-squares 3D point positions stored in Google Sheets",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "serve": "node tools/serve.mjs"
  }
}
